[tox]
skipsdist = True

envlist =
    py37
	py38

[testenv]
ignore_errors = True

envdir =
    py37: {env:ENVDIR:.}/.venv-py37
    py38: {env:ENVDIR:.}/.venv-py38

deps =
	flake8
	pylint
	mypy

commands =
	flake8 --version
	flake8 {{ project_name }}/ setup.py

	pylint --version
	pylint {{ project_name }}/

	mypy --version
	mypy -p {{ project_name }}
